---
'@ankhorage/supabase': patch
---

Honor the canonical auth sign-up policy when configuring GoTrue email confirmation so `autoSignIn` users are confirmed immediately and `requireVerification` keeps confirmation required.
